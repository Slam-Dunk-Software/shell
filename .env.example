# Copy this file to .env and set your values before running epm services start.

# PIN for the lock screen (4 digits recommended)
SHELL_TOKEN=1234

# Optional: attach to a specific tmux session by name
# TMUX_SESSION=main

# Optional: SSH key used to spawn the terminal (default: ~/.ssh/shell_key)
# SSH_KEY_PATH=~/.ssh/shell_key
